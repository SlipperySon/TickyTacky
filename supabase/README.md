# Tickytacky Supabase

Canonical backend for Tickytacky.
Create/link a project with the Supabase CLI when ready:

```bash
supabase login
supabase init  # if needed
supabase link --project-ref <ref>
```

Migrations live in `migrations/`.
