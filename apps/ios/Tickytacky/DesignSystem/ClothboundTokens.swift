import SwiftUI

/// Charcoal cloth, parchment ink, kraft gilt.
/// Source of truth: DESIGN.md → Clothbound.
struct ClothboundTokens: ThemeTokens {
    let canvas = Color(hex: 0x1A1B1C)
    let canvasRuled = Color(hex: 0x222324)
    let surface = Color(hex: 0x141516)
    let surfaceInk = Color(hex: 0x2A2B2C)
    let ink = Color(hex: 0xE3D4C3)
    let inkMuted = Color(hex: 0xA89880)
    let inkFaint = Color(hex: 0x8A8074)
    let rule = Color(hex: 0x3A3B3D)
    let ruleNotebook = Color(hex: 0x3E3F41)
    let accent = Color(hex: 0xC4A070)
    let accentPressed = Color(hex: 0x917656)
    let accentSoft = Color(hex: 0x2C2A26)
    let accentSecondary = Color(hex: 0xC4A882)
    let accentSecondarySoft = Color(hex: 0x2A2824)
    let todayMark = Color(hex: 0xC4A070)
    let danger = Color(hex: 0xC46B5D)
    let overdue = Color(hex: 0xC47A6C)
    let warning = Color(hex: 0xD4B56A)
}
