import SwiftUI

/// Leather-bound dark: cocoa paper, parchment ink, kraft chrome.
/// Source of truth: DESIGN.md → Morocco.
struct MoroccoTokens: ThemeTokens {
    let canvas = Color(hex: 0x1C1612)
    let canvasRuled = Color(hex: 0x241C16)
    let surface = Color(hex: 0x2A221C)
    let surfaceInk = Color(hex: 0x322820)
    let ink = Color(hex: 0xE3D4C3)
    let inkMuted = Color(hex: 0xA89880)
    let inkFaint = Color(hex: 0x8A7A68)
    let rule = Color(hex: 0x3A322C)
    let ruleNotebook = Color(hex: 0x4A3E34)
    let accent = Color(hex: 0xC4A070)
    let accentPressed = Color(hex: 0x917656)
    let accentSoft = Color(hex: 0x3D3228)
    let accentSecondary = Color(hex: 0xC4A882)
    let accentSecondarySoft = Color(hex: 0x3A3228)
    let todayMark = Color(hex: 0xC4A070)
    let danger = Color(hex: 0xC46B5D)
    let overdue = Color(hex: 0xC47A6C)
    let warning = Color(hex: 0xD4B56A)
}
